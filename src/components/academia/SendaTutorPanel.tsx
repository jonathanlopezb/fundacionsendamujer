'use client';
import React, { useState } from 'react';
import { MessageCircle, Send, ShieldCheck } from 'lucide-react';
export default function SendaTutorPanel({ courseTitle }: { courseTitle: string }) { const [question, setQuestion] = useState(''); const [answer, setAnswer] = useState(''); const ask = () => { if (!question.trim()) return; setAnswer(`Según el contenido autorizado de “${courseTitle}”, revisa la lección activa y el material descargable. SendaTutor no reemplaza orientación médica, jurídica ni atención de crisis.`); setQuestion(''); }; return <div className="space-y-4 rounded-2xl border border-fuchsia-300/15 bg-white/[.04] p-5"><div className="flex items-center gap-2"><MessageCircle className="h-4 w-4 text-fuchsia-300" /><h4 className="text-xs font-black text-white">Pregúntale a SendaTutor</h4></div><p className="text-[10px] leading-5 text-slate-400">Tutor limitado al contenido oficial de este curso. No accede a expedientes ni datos sensibles.</p><textarea value={question} onChange={(e) => setQuestion(e.target.value)} placeholder="No entendí cómo aplicar este concepto…" className="min-h-24 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-xs text-white outline-none focus:border-fuchsia-400" /><button onClick={ask} className="inline-flex items-center gap-2 rounded-full bg-fuchsia-600 px-4 py-2 text-xs font-bold text-white"><Send className="h-3.5 w-3.5" /> Preguntar</button>{answer && <div className="rounded-xl border border-emerald-300/20 bg-emerald-300/5 p-3 text-xs leading-5 text-emerald-100"><ShieldCheck className="mb-1 h-4 w-4 text-emerald-300" />{answer}</div>}</div>; }
